# Auto-generated from templates/specs/services/gitlab.yaml — do not edit directly.
# Edit the spec and regenerate; see tools/README.md.

services:
  gitlab:

    secrets:
      - smtp_password

secrets:
  smtp_password:
    file: ${SECRETS_DIR:?SECRETS_DIR is required}/env/000-generic/smtp/password
