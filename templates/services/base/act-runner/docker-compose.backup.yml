# Auto-generated from templates/specs/services/act-runner.yaml — do not edit directly.
# Edit the spec and regenerate; see tools/README.md.

include:
  - ../../custom/backup/docker-compose.yml

x-volumes: &act-runner-volumes
  volumes:
    - act-runner_data:/mnt/act-runner

services:
  backup: *act-runner-volumes
  restore:
    <<: *act-runner-volumes
    configs:
      - source: act-runner_restore_00_owner
        target: /srv/configs/restore.d/00-act-runner-owner.sh

configs:
  act-runner_restore_00_owner:
    content: |
      set -e
      chown -R 1000:1000 /mnt/act-runner
      find /mnt/act-runner -type d -exec chmod 700 {} +
      find /mnt/act-runner -type f -exec chmod 600 {} +
