# Auto-generated from templates/specs/services/mysql.yaml — do not edit directly.
# Edit the spec and regenerate; see tools/README.md.

include:
  - ../../custom/backup/docker-compose.yml

x-volumes: &mysql-volumes
  volumes:
    - mysql_data:/mnt/var/lib/mysql

services:
  backup: *mysql-volumes
  restore:
    <<: *mysql-volumes
    configs:
      - source: mysql_restore_00_owner
        target: /srv/configs/restore.d/00-mysql-owner.sh

configs:
  mysql_restore_00_owner:
    content: |
      set -e
      chown -R 999:999 /mnt/var/lib/mysql
      find /mnt/var/lib/mysql -type d -exec chmod 700 {} +
      find /mnt/var/lib/mysql -type f -exec chmod 600 {} +
