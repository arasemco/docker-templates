# Auto-generated from templates/specs/services/wordpress.yaml — do not edit directly.
# Edit the spec and regenerate; see tools/README.md.

include:
  - ../backup/docker-compose.yml

x-volumes: &wordpress-volumes
  volumes:
    - wordpress_data:/mnt/var/www/wordpress

services:
  backup: *wordpress-volumes
  restore:
    <<: *wordpress-volumes
    configs:
      - source: wordpress_restore_00_owner
        target: /srv/configs/restore.d/00-wordpress-owner.sh

configs:
  wordpress_restore_00_owner:
    content: |
      set -e
      chown -R 33:33 /mnt/var/www/wordpress
      find /mnt/var/www/wordpress -type d -exec chmod 755 {} +
      find /mnt/var/www/wordpress -type f -exec chmod 644 {} +
