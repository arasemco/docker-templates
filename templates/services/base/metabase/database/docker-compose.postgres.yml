# Auto-generated from templates/specs/services/metabase.yaml — do not edit directly.
# Edit the spec and regenerate; see tools/README.md.

services:
  metabase:
    environment:
      MB_DB_TYPE: postgres
      MB_DB_HOST: ${POSTGRES_HOST:-postgres}
      MB_DB_PORT: ${POSTGRES_PORT:-5432}
      MB_DB_DBNAME: ${POSTGRES_NAME:?POSTGRES_NAME is required}
      MB_DB_USER: ${POSTGRES_USER:?POSTGRES_USER is required}
      MB_DB_PASS_FILE: /run/secrets/postgres_password

    secrets:
      - postgres_password

secrets:
  postgres_password:
    file: ${SECRETS_DIR:?SECRETS_DIR is required}/env/${COMPOSE_PROJECT_NAME}/postgres_password
