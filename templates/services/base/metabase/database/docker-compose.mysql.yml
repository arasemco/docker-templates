# Auto-generated from templates/specs/services/metabase.yaml — do not edit directly.
# Edit the spec and regenerate; see tools/README.md.

services:
  metabase:
    environment:
      MB_DB_TYPE: mysql
      MB_DB_HOST: ${MYSQL_HOST:-mysql}
      MB_DB_PORT: ${MYSQL_PORT:-3306}
      MB_DB_DBNAME: ${MYSQL_NAME:?MYSQL_NAME is required}
      MB_DB_USER: ${MYSQL_USER:?MYSQL_USER is required}
      MB_DB_PASS_FILE: /run/secrets/mysql_password

    secrets:
      - mysql_password

secrets:
  mysql_password:
    file: ${SECRETS_DIR:?SECRETS_DIR is required}/env/${COMPOSE_PROJECT_NAME}/mysql_password
