# Auto-generated from templates/specs/services/mariadb.yaml — do not edit directly.
# Edit the spec and regenerate; see tools/README.md.

include:
  - ../backup/docker-compose.yml

x-volumes: &mariadb-volumes
  volumes:
    - mariadb_data:/mnt/var/lib/mariadb

services:
  backup: *mariadb-volumes
  restore:
    <<: *mariadb-volumes
    configs:
      - source: mariadb_restore_00_owner
        target: /srv/configs/restore.d/00-mariadb-owner.sh

configs:
  mariadb_restore_00_owner:
    content: |
      set -e
      chown -R 999:999 /mnt/var/lib/mariadb
      find /mnt/var/lib/mariadb -type d -exec chmod 700 {} +
      find /mnt/var/lib/mariadb -type f -exec chmod 600 {} +
