# Auto-generated from templates/specs/services/dsh.yaml — do not edit directly.
# Edit the spec and regenerate; see tools/README.md.

include:
  - ../../custom/backup/docker-compose.yml

x-volumes: &dsh-volumes
  volumes:
    - dsh_home:/mnt/home/dsh/.dsh

services:
  backup: *dsh-volumes
  restore:
    <<: *dsh-volumes
    configs:
      - source: dsh_restore_00_owner
        target: /srv/configs/restore.d/00-dsh-owner.sh

configs:
  dsh_restore_00_owner:
    content: |
      set -e
      chown -R 1000:1000 /mnt/home/dsh/.dsh
      find /mnt/home/dsh/.dsh -type d -exec chmod 700 {} +
      find /mnt/home/dsh/.dsh -type f -exec chmod 600 {} +
