image: ubuntu/squid
tag_default: "latest"

cpu_default: "0.5"
mem_default: "256M"

extra:
  profiles:
    - ""
    - squid
    - forward-proxy
    - fwd-proxy

labels:
  homepage.:
    description: "Forward/caching HTTP proxy"

header_comment: |
  Squid — forward/caching HTTP proxy.
  Config (squid.conf/conf.d/lists) and ports are deployment-specific —
  bind-mount and publish them in the consuming docker-compose.yml.
